node_modules/
dist/

# Bun compiled binary (anchored to repo root so plugins/chartjs2img is not blocked)
/chartjs2img
/chartjs2img-darwin-arm64

# Output files (example renders are regenerable via `chartjs2img examples`)
*.png
*.jpg
*.jpeg
*.webp
examples/

# Bun
bun.lock

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# VitePress build artifacts
docs/.vitepress/cache/
docs/.vitepress/dist/

# AI artifacts generated at build time (see PLAN.md §2)
docs/public/examples/
docs/public/diagrams/
docs/public/llms.txt
docs/public/llms-full.txt
src/llm-docs/.generated-reference.md

# Claude Code local state (commit rules/ and skills/, ignore per-user state)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/cache/

# Local scratch (editor dumps, quick diagram trials, etc.)
tmp/
