{
  "name": "chartjs2img",
  "version": "0.1.0",
  "description": "Render Chart.js charts to images via CLI or HTTP API using Playwright",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "chartjs2img": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts serve",
    "build": "bun build src/index.ts --compile --outfile chartjs2img --external electron --external chromium-bidi/lib/cjs/bidiMapper/BidiMapper --external chromium-bidi/lib/cjs/cdp/CdpConnection",
    "start": "bun run src/index.ts serve"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
